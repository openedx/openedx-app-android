<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black">

    <!-- Draws the camera preview plus zxing's framing viewfinder (the scan rectangle). -->
    <com.journeyapps.barcodescanner.DecoratedBarcodeView
        android:id="@+id/barcode_scanner"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:zxing_use_texture_view="true" />

    <!-- Explicit exit affordance: the camera is full-screen, so a visible Close is needed. -->
    <ImageButton
        android:id="@+id/qr_close"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:layout_gravity="top|start"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        android:background="@drawable/auth_qr_close_bg"
        android:contentDescription="@string/auth_lms_qr_close"
        android:padding="10dp"
        android:scaleType="fitCenter"
        android:src="@drawable/auth_ic_qr_close" />
</FrameLayout>
